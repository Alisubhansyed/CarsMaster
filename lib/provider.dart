import 'package:flutter/material.dart';

class Providered extends StatelessWidget {
  const Providered({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
